[package]
edition = "2018"
name = "rolling_stats"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = []
reconstructor = []

[dependencies]
approx = "0.5.0"
rand = "0.8.0"
rand_distr = "0.4.0"
thiserror = "1.0"

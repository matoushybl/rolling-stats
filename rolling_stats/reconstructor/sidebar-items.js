initSidebarItems({"struct":[["Reconstructor","Reconstructor is a structure that holds all of the intermediate buffers when receiving data using the `std::io::Write` trait. It depends on the raw converter represented by the type parameter `E`. The output type is denoted T. The intermediate result (parsed `T`s) are contained in the Reconstructor itself and are than retrieved using an iterator and cleared via the `std::io::Write` `flush` method."]]});